
const desc=document.getElementById('headerDesc'); const about=document.createElement('details');about.className='study-about';about.innerHTML='<summary>About this culture</summary>';about.append(desc.cloneNode(true));document.querySelector('.page').append(about);desc.textContent='Three colonies. One page. Paint into the dish and watch a reading emerge.';
const tools=document.createElement('div');tools.className='quick-tools';tools.append(document.querySelector('.swatch-row'),document.getElementById('runBtn'),document.getElementById('captureBtn'));document.querySelector('header').append(tools);
const rail=document.querySelector('.rail');const drawer=document.createElement('details');drawer.className='instrument-drawer';drawer.innerHTML='<summary>Open the instrument · growth, evolution & source text</summary>';rail.before(drawer);drawer.append(rail);

const optics=document.createElement('div');optics.className='optics-bar';optics.innerHTML='<div class="optics-modes" role="group" aria-label="View the culture"><button type="button" data-optic="fluorescence" aria-pressed="true">Fluorescence</button><button type="button" data-optic="relief" aria-pressed="false">Relief</button><button type="button" data-optic="soft" aria-pressed="false">Soft light</button></div><label>Exposure <input aria-label="Optical exposure" type="range" min="0.5" max="2" step="0.05" value="1.2"></label>';
document.getElementById('fieldWrap').after(optics);
optics.querySelectorAll('[data-optic]').forEach(button=>button.addEventListener('click',()=>{optics.querySelectorAll('[data-optic]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));window.BloomOptics.setMode(button.dataset.optic);}));optics.querySelector('input').addEventListener('input',e=>window.BloomOptics.setExposure(e.target.value));
